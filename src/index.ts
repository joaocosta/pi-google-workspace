import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/** Pi package composition root. Feature registration is added by app modules. */
export default function googleWorkspace(_pi: ExtensionAPI): void {}
